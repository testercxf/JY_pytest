{
  "name": "jy-pytest-dummy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "python3 -m ensurepip --upgrade && python3 -m pip install --upgrade pip && pip3 install -r requirements.txt && python3 run.py"
  }
}
